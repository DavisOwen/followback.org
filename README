After cloning repository, set up virtual environment with 

$ python -m venv flask

for python 3

or 

$ virtualenv flask

for python 2

Then either

$ flask/bin/pip install -r requirements.txt

or

$ source flask/bin/activate
$ (flask) pip install -r requirements.txt
